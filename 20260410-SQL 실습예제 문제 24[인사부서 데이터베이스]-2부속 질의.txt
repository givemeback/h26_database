⓵전체 직원 평균 급여보다 많이 받는 직원의 last_name과 salary를 출력하시오.

select ename,sal
from emp
where sal > (
select avg(sal)
from emp);


⓶dea hann과 같은 job_id를 가진 직원의 last_name과 job_id를 출력하시오.

select ename,job
from emp
where job = (
select job
from emp 
where ename = 'den hann');


⓷부서별 최고 급여를 받는 직원의 last_name, department_id를 출력하시오.

select ename,deptno
from emp 
where (deptno,sal) in (
select deptno,max(sal)
from emp 
group by deptno);



⓸IT부서 직원의 평균 급여보다 많이 받는 직원의  last_name과 salary를 출력하시오.

select ename,sal
from emp 
where sal > (
select avg(e.sal)
from emp e
join dept d on e.deptno = d.deptno
where dname = 'IT');


⑤직무이력(JOB_HISTORY)이 있는 직원의 last_name과 현재 job_id를 출력하시오.

SELECT last_name, job_id
FROM employees
WHERE employee_id IN (
    SELECT employee_id 
    FROM job_history);

⓺직무이력이 없는 직원의 last_name과 employee_id를 출력하시오.

SELECT last_name, job_id
FROM employees
WHERE employee_id NOT IN (
    SELECT employee_id 
    FROM job_history);



⓻급여가 자신이 속한 부서 평균보다 높은 직원의 이름,급여,부서번호를 출력하시오(상관부속 질의)

select e.ename,e.sal,e.deptno
from emp e
where e.sal >(
select avg(s.sal)
from emp s
where e.deptno = s.deptno);



⓼kochhar(101)를 관리자로 두는 직원의 이름과 급여를 출력하시오.

select ename,sal
from emp 
where mgr = (
select empno
from emp
where ename = 'kochhar');



⓽급여 최상위 3명의 last_name과 salary를 출력하시오.

select ename,sal
from emp 
order by sal desc
limit 3;



⓾FI_ACCOUNT 직원 중 급여가 FI_ACCOUNT 평균보다 높은 직원을 출력하시오.

select ename
from emp 
where job ='FI_ACCOUNT'
and 
sal >(
select avg(sal)
from emp 
where job ='FI_ACCOUNT');




